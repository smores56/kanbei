[package]
name = "kb-guest"
version = "0.0.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
luaur = { version = "0.1.8", default-features = false, features = ["send"] }
