[package]
name = "kanbei-policy"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
kanbei-core.workspace = true
# Pure-Rust regex (no C), wasm-compatible (the deferred no-effect policy
# runtime is the wasm hosting path, R-28/D-S3), and panic-free on input —
# fits the no-effect policy seam. Version "1" resolves from the local cache.
regex = "1"
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
