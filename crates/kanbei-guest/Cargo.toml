[package]
name = "kanbei-guest"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
luaur = { version = "0.1.8", default-features = false, features = ["send"] }
